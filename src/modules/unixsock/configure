#!/bin/sh

if [ "$help" != "1" ]
then

	pkg-config libunixsock 2> /dev/null
	disable_libunixsock=$?

	if [ "$disable_libunixsock" != "0" ]
	then
		echo "- libunixsock not found: disabling"
		touch ../disable-unixsock
		exit 0
	fi

fi

